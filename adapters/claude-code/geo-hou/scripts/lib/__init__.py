"""GEO-HOU core library (stdlib only).

Modules:
  htmldoc    -- parse an HTML document into a structured, analyzable form
  scoring    -- the 6-dimension / 22-check GEO citability score card
  generators -- robots.txt / llms.txt / JSON-LD schema generators
"""

__version__ = "1.0.0"
